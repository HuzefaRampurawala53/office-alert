{
  "name": "office-alert",
  "private": true,
  "scripts": {
    "client": "npm run dev --prefix client/office-alert",
    "server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "db:wipe": "npm run db:wipe --prefix server"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "react-icons": "^5.7.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
